diff --git a/app/Http/Controllers/LARSController.php b/app/Http/Controllers/LARSController.php
new file mode 100644
index 000000000..a5dec2bd5
--- /dev/null
+++ b/app/Http/Controllers/LARSController.php
@@ -0,0 +1,63 @@
+<?php
+
+namespace App\Http\Controllers;
+
+use Illuminate\Support\Facades\Auth;
+
+use App\Models\Setting;
+use App\Helpers\Helper;
+
+/**
+ * This class controls all actions related to qr codes for assets
+ * the Snipe-IT Asset Management application.
+ *
+ * @version v0.2
+ * @author Aron Heinecke
+ */
+class LARSController extends Controller
+{
+    protected $qrCodeDimensions = array('height' => 3.5, 'width' => 3.5);
+    protected $barCodeDimensions = array('height' => 2, 'width' => 22);
+
+    public function __construct()
+    {
+        parent::__construct();
+    }
+
+
+    /**
+     * Return login json data as string
+     *
+     * @author Aron Heinecke
+     * @return Response
+     */
+    private function getLoginJson()
+    {
+        $id = Auth::id();
+        $user = Auth::user();
+        $user->tokens->each(function($token, $key) {
+            if ($token->name == 'LARS-App') {
+                $token->delete();
+            }
+        });
+        $token = $user->createToken('LARS-App')->accessToken;
+        $url = \App::make('url')->to('/');
+        
+        return json_encode(["uid" => $id, "url" => $url, "token" => $token ]);
+    }
+    
+    /**
+     * Return a QR code for the asset
+     *
+     * @author Aron Heinecke
+     * @return Response
+     */
+    public function getQRCode()
+    {
+        $data = $this->getLoginJson();
+        
+        $barcode = new \Com\Tecnick\Barcode\Barcode();
+        $barcode_obj = $barcode->getBarcodeObj('QRCODE', $data, 921,921, 'black', array(-5, -5, 0, -5));
+        return response($barcode_obj->getSvgCode())->header('Content-type', 'image/svg+xml');
+    }
+}
diff --git a/resources/views/layouts/default.blade.php b/resources/views/layouts/default.blade.php
index 28c2bddb9..36923ccf6 100644
--- a/resources/views/layouts/default.blade.php
+++ b/resources/views/layouts/default.blade.php
@@ -353,6 +353,11 @@
                              <i class="fa fa-user-secret fa-fw" aria-hidden="true"></i> Manage API Keys
                          </a>
                      </li>
+                     <li>
+                         <a href="{{ route('user.lars') }}">
+                             <i class="fa fa-user-secret fa-fw"></i> LARS App Login
+                         </a>
+                     </li>
                      @endcan
                      <li class="divider"></li>
                      <li>
diff --git a/routes/web.php b/routes/web.php
index aa9631475..9cc6d3c3c 100644
--- a/routes/web.php
+++ b/routes/web.php
@@ -245,6 +245,8 @@ Route::group([ 'prefix' => 'account', 'middleware' => ['auth']], function () {
     Route::post('password', [ 'uses' => 'ProfileController@passwordSave' ]);
 
     Route::get('api', [ 'as' => 'user.api', 'uses' => 'ProfileController@api' ]);
+    
+    Route::get('lars', ['as' => 'user.lars', 'uses' => 'LARSController@getQRCode']);
 
     # View Assets
     Route::get('view-assets', [ 'as' => 'view-assets', 'uses' => 'ViewAssetsController@getIndex' ]);
